export enum pageName {
  SEARCHBOOK = "searchbook",
  DATABOOK = "databook",
  DATATABLES = "datatables",
  CONFIRM = "confirm",
  ERROR = "error",
}

export enum actionName {
  BACK = "back",
  AHEAD = "ahead",
}
